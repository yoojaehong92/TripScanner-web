import React from "react";
import LoginForm from "../Components/LoginForm"

const Login = () => {
    return (
        <div>
            <LoginForm/>
            Login
        </div>
    );
};

export default Login;